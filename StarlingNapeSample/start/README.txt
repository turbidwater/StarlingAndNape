Set up as an ActionScript Project
- Add the Starling src folder as a Source Path in Properties > ActionScript Build Paths > Source Paths.
- Add the libs folder as a SWC Folder in Properties > ActionScript Build Paths